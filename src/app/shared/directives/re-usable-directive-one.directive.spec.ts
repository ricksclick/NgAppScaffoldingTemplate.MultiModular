import { ReUsableDirectiveOneDirective } from './re-usable-directive-one.directive';

describe('ReUsableDirectiveOneDirective', () => {
  it('should create an instance', () => {
    const directive = new ReUsableDirectiveOneDirective();
    expect(directive).toBeTruthy();
  });
});
