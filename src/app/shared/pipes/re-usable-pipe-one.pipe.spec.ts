import { ReUsablePipeOnePipe } from './re-usable-pipe-one.pipe';

describe('ReUsablePipeOnePipe', () => {
  it('create an instance', () => {
    const pipe = new ReUsablePipeOnePipe();
    expect(pipe).toBeTruthy();
  });
});
